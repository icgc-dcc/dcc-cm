---

# - include: dependencies.yml
# - include: install.yml
- name: Check CM service existence and state
  command:  service cloudera-scm-server status
  register: cm_started
  ignore_errors: True

- include: installer.yml
  when: cm_started.stderr.find('unrecognized service') != -1

- include: configure.yml host='cloudera_manager'
